#!/usr/bin/with-contenv bash

_term() {
  echo "Caught SIGTERM signal!"
  wg-quick down wg0
}

trap _term SIGTERM

wg-quick up wg0

if [ "true" == "$LOG" ]; then
  echo "**** Logging activated ****"
  /app/logging.sh
else
  sleep infinity &
  wait
fi
